

## overview

To test the compatibility when a low version is updated to a higher version

## scope

Make sure that the data inserted in old version can be searched in a new version (both standalone and cluster)
Now focus on docker-compose installation, will cover the k8s
## implement


issue:
how to get the latest tag of milvus images

how to get the status of each container (we need to wait all containers is running(or heathy), then we can execute our tests)

